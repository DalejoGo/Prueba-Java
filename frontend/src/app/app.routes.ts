import { Routes } from '@angular/router'; // Importa Routes de Angular Router

export const routes: Routes = []; // Exporta una constante llamada routes que es un array de rutas (vacío en este caso)
