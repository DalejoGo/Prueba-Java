import { NgModule } from '@angular/core'; // Importa NgModule de Angular
import { CommonModule } from '@angular/common'; // Importa CommonModule de Angular

@NgModule({
  declarations: [], // Declaraciones de componentes, directivas y pipes (vacío en este caso)
  imports: [
    CommonModule // Importa CommonModule para tener acceso a las directivas comunes de Angular
  ]
})
export class AppRoutingModule { } // Define y exporta la clase AppRoutingModule
